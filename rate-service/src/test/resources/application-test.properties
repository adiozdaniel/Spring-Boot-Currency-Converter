# Test application properties
spring.application.name=rate-service
server.port=0

# Disable Spring Cloud Config for tests
spring.cloud.config.enabled=false

# Exchange Rate API configuration for tests
exchange-rate-api.url=https://api.exchangerate-api.com/v4/latest
exchange-rate-api.connect-timeout=5000
exchange-rate-api.read-timeout=5000

# Logging
logging.level.com.currencyconverter.rateservice=DEBUG
