# Rate Service - Production Environment

logging.level.root=WARN
logging.level.com.example=INFO

exchange-rate.api.key={cipher}PLACEHOLDER_PROD_EXCHANGE_RATE_KEY
exchange-rate.api.timeout=3000
exchange-rate.api.cache-ttl=7200  # 2 hours for production

security.api.key={cipher}PLACEHOLDER_PROD_INTERNAL_API_KEY

cache.ttl=7200  # 2 hours
cache.max-size=5000  # Larger cache for production
