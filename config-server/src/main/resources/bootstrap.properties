# Encryption configuration
# For symmetric encryption, set ENCRYPT_KEY environment variable
# For asymmetric encryption, use keystore configuration in application.properties
encrypt.key=${ENCRYPT_KEY}
