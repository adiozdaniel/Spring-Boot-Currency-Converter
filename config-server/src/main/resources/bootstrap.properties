# Encryption configuration
# For symmetric encryption, set ENCRYPT_KEY environment variable
# For asymmetric encryption, use keystore configuration
encrypt.key=${ENCRYPT_KEY:changeme-default-encryption-key}
# Alternatively, use keystore for production
# encrypt.key-store.location=classpath:/config-server.jks
# encrypt.key-store.password=${KEYSTORE_PASSWORD}
# encrypt.key-store.alias=config-server-key
# encrypt.key-store.secret=${KEY_PASSWORD}
